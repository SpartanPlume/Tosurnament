services:
  db:
    ports:
      - ${DB_TESTING_PORT}:5432
  api:
    ports:
      - ${API_TESTING_PORT}:8080
    environment:
      - APP_ENVIRONMENT=testing
  web:
    ports:
      - ${WEB_TESTING_PORT}:8080
    environment:
      - APP_ENVIRONMENT=testing
